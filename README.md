# The Nine Puzzle in Prolog

 1. Clone the repo.
 2. For SWI-Prolog, do `$ swipl -s nine.pl` followed by `?- plunit:run_tests.`.
 3. You can also try `?- nine([a,m,n, l,k,a, d,s,r], Word).` for generation.
